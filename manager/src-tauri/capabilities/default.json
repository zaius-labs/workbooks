{
  "$schema": "https://schema.tauri.app/config/capabilities",
  "identifier": "default",
  "description": "Capability set for the main window. Frontend can invoke our daemon_url command and otherwise relies on standard browser fetch() to call the local workbooksd HTTP API.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default"
  ]
}
