{
  "name": "@workbooks/manager",
  "private": true,
  "version": "0.1.0",
  "description": "Desktop app for browsing every workbook the daemon's ledger knows about — search, history, jump-to-latest. Tauri + Svelte 5.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tauri-apps/cli": "^2.10.0",
    "svelte": "^5.0.0",
    "vite": "^5.4.0"
  }
}
