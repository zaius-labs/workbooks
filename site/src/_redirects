# Cloudflare Pages redirects.
#
# (Pivot 2026-05-04 — the legacy `/dl/*` daemon-download redirect was
# removed. Workbooks ships as portable HTML now; the CLI builds them.
# See vendor/workbooks/CLAUDE.md → "Workbooks pivot (W-series)".)

# Format: <source-path>  <destination-url>  <status-code>
