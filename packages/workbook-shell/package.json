{
  "name": "@work.books/shell",
  "version": "0.1.0",
  "description": "Workbooks PWA shell — registered as the OS file handler for .workbook.html files; runs the substrate runtime on launched workbooks",
  "type": "module",
  "license": "Apache-2.0",
  "files": [
    "public",
    "src",
    "README.md"
  ],
  "scripts": {
    "preview": "node scripts/serve.mjs",
    "deploy": "wrangler pages deploy public --project-name workbooks-shell --branch main --commit-dirty=true"
  }
}
